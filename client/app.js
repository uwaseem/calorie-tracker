const uploadBtn = document.getElementById("uploadBtn")
const results = document.getElementById("results")

uploadBtn.addEventListener("click", () => {
  results.textContent = "Thank you for clicking the upload button! We have not implemented the upload functionality yet, but we will soon. Stay tuned!"
})